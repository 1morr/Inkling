# Changelog

本檔案遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 的格式。

## [Unreleased]

首次公開發佈前的修正輪(內部審查,尚未發版)。

### 新增

- **隨手草稿**(頂層命令 `Inkling：隨手草稿`):一塊永久的便條紙,打開就是上次留下的東西,
  不必取標題、不必挑檔案。給的是「還沒成形、不值得變成一則筆記」的想法一個去處。
  存成筆記資料夾根目錄的 `scratchpad.md`(純文字、沒有 front matter),跟著雲端同步走,
  但**不會出現在筆記清單與搜索裡**。
  **沒有自動儲存** —— CmdPal 的表單值只有在提交時才回到擴展,做不到(底部工具列也因此
  放不了「儲存」:那兩顆按鈕沒有參數可拿)。存檔是 `Tab` → `Enter`,**存完面板自己收起來**,
  兩鍵結束。要真正的自動儲存就按 `Ctrl+O`(或 `Ctrl+Enter`)跳到系統預設編輯器,
  那一頁會順手收起來,免得回來時用舊值蓋掉外部的修改。`Enter` 是「捨棄變更」——
  丟掉的是這一趟的編輯,不是檔案裡的草稿。
  考證見[設計考證〈隨手草稿為什麼沒有自動儲存〉](docs/design-notes.md#scratchpad-no-autosave)。
- **瀏覽筆記時按 `Ctrl+N` 直接開新增筆記的表單**,不必先退回主搜尋框。
  它出現在每一則筆記的 `Ctrl+K` 選單裡(排在「開啟檔案位置」與「刪除」之間)——
  CmdPal 的快速鍵只認當下選中項的命令,頁面層級沒有掛鍵的地方。
- **`Ctrl+U` 現在在預覽頁也能用**,而且切的是整頁內容:原文走 CmdPal 的純文字檢視器
  (`PlainTextContent`),等寬字、**縮排與連續空行一個字元都不動**,右鍵還能切自動換行與縮放。
  這解掉了「內文是 HTML / SVG 的筆記,預覽頁看起來完全空白」—— 那些標記會被 Markdown
  渲染器整段吃掉,內容其實一直都在。
- **原始文字模式改成全域狀態,而且會記住**:在預覽頁切掉,回清單頁的詳細窗格也是原文;
  關掉 CmdPal、重載擴展都還在(存在 `settings.json` 的 `Inkling.ShowSource`)。
  刪除筆記那一頁的詳細窗格也跟著走。設定頁上刻意沒有這一項 —— 切換鍵就是它的介面。

### 變更

- **底部工具列那兩顆按鈕的順序整理過了。** 它們是**位置鍵** —— 主命令(`Enter`)與次要命令
  (`Ctrl+Enter`)坐上去的是誰**只看命令排序**,跟命令自己綁的快速鍵無關。以前預覽頁的
  `Ctrl+Enter` 順位落在「複製內文」上,跟另外兩頁不同義。現在:
  - **清單頁**與**記下並預覽頁**:`Enter` 是那一頁真正的下一步(開啟 / 完成),
    `Ctrl+Enter` 是「編輯」。
  - **預覽頁刻意相反**:`Enter` 是「編輯」,`Ctrl+Enter` 是「完成(收起面板)」。
    是在清單裡找到某一則才進來的,下一步多半是改它 —— 讓 `Enter` 變成「把面板收掉」的
    代價比跨頁不一致大。
  - 編輯照樣有 `Ctrl+E`,複製內文照樣有 `Ctrl+Shift+C`。
  - `Inkling：刪除筆記` 那一頁刻意不跟(它的兩個鍵是「先問一次」與「直接刪」)。

### 重新命名:Notelet → Inkling

專案改名了。趁尚未發版一次改到底 —— 套件身分、namespace、資料夾、腳本、文檔全部跟著換。

- 換名字的原因是搜尋:在 Command Palette 打 `note` 時 Notelet 會排在 Notepad 前面,
  等於這個擴展在擋使用者要開的東西。`ink` 這一帶乾淨得多。
- **`%LOCALAPPDATA%\Packages\<PFN>\LocalState` 換了位置**(PFN 從
  `Notelet_bf0n0751x5hse` 變成 `Inkling_bf0n0751x5hse`),舊的 `settings.json` 成為孤兒 ——
  筆記資料夾、快速記下分隔符、記下後預覽這三項會回到預設值,重設一次即可。
- **預設筆記資料夾也跟著改名**,從 `<OneDrive>\Notelet` 變成 `<OneDrive>\Inkling`。
  升級前用過預設資料夾的話,**把舊資料夾改名,或在設定裡把路徑指回舊的** ——
  否則筆記檔案都還在,清單卻會是空的。
- **Command Palette 端設過的 alias 不受影響。** CmdPal 的 `Aliases` 是以純命令 Id 當鍵,
  而那六個 Id 刻意保留原字串(仍是 `Notelet.*`)。釘選與擴展層級的啟用狀態會回到預設。
- MSIX 靠套件身分認「同一個套件的新版本」,所以舊的 Notelet 不會被取代。
  用 `Get-AppxPackage Notelet | Remove-AppxPackage` 移掉,否則兩個並存。

### 變更(圖示)

- 全新的圖示:一道有壓感的下筆加一顆句點,取自 inkling 的字面。
  刻意避開捲角便條紙 / 鉛筆加紙 / 記事本 —— 那是 Notepad、Sticky Notes、OneNote 的符號。
- **頂層命令改用自訂的單色圖示**(清單 / 快速記下 / 新增筆記 / 隨手草稿 / 刪除筆記),
  同一個家族,右下角換修飾符。`Ctrl+K` 選單與頁面內維持 Segoe Fluent 字形。
- 自訂圖示每個備了淺色與深色主題兩張 PNG —— 字形會自動跟主題變色,PNG 不會。
- 代價講明白:刪除的圖示變成「筆畫＋叉」,不如垃圾桶一望即知。這是為了家族一致
  刻意付的;改回 `Glyph(0xE74D)` 只是一行的事。
- 套件圖示補齊 `Square44x44Logo` 的兩條候選階梯(`scale-100/125/150/200/400` 與
  `targetsize-16/24/32/48/256_altform-unplated`)。模板只給了 88px 與 24px 兩張,
  而要 unplated 的地方只有 24px 可挑 —— 在 150% DPI 下被放大,看起來是糊的。

### 修正

- **失敗提示現在真的會出現。** 「檔案不在了」「用預設編輯器開不起來」這類訊息走的是
  `ToastStatusMessage`,而它需要擴展先在 `InitializeWithHost` 裡把 CmdPal 交過來的 host
  接給 `ExtensionHost` —— 我們一直沒接,於是 `Show()` 靜靜地什麼都不做,不丟例外也不留痕跡。
  也就是說**那條「看得見的失敗提示」從頭到尾一句都沒有真的送到畫面上**,而文檔一直
  把它寫成通的。接上之後訊息畫成一條橫跨面板底部的 InfoBar 加一個計數徽章,面板不會關掉。
- **讀不出來的筆記檔不再靜靜從清單上消失。** 被別的程式獨佔開著、或編碼壞掉的 `.md`
  會被跳過,而使用者沒有任何線索 —— 現在清單最後多一列「有 N 個檔案讀不出來」,
  跟「還有幾則沒顯示」同一個形狀。那個數字 repository 一直有在數,只是以前沒有人讀它。
- **診斷日誌不再逐字記下筆記內文,只記字數。** bug 回報範本請使用者把整份 log 貼進
  **公開的** issue,而那份 log 記著剛打完的整段內文 —— 那是這個專案裡唯一會主動導致
  外洩的路徑。字數對排錯就夠了(那一行本來是拿來確認標題與內文切在哪裡的)。
  標題與搜尋字串照樣記(少了就對不上是哪一則),範本因此加上了去識別化的提醒,
  並把「log 裡有哪些東西」講清楚。
- **複製內文失敗時不再回報「已複製」。** toolkit 的剪貼簿輔助方法把失敗吞掉,而這裡
  無條件說成功 —— 剪貼簿被別的程式鎖住時,那一列會打出「已複製」而剪貼簿裡還是舊東西。
  現在寫完讀回來比一次,對不上就說「複製不成功」。
- **設定存不進磁碟時不再顯示「設定已儲存」。** 以前那個失敗只寫進預設關閉的診斷日誌,
  使用者看到的是成功訊息,下次打開卻是舊值。現在會講清楚「暫時生效了,但重啟之後會還原」。
- 資料夾監看的通知回呼現在會攔下訂閱者丟出來的例外。執行緒池上沒接住的例外會**直接
  終止整個擴展進程**,而訂閱者呼叫的是跨進程的 CmdPal —— 對方走掉之後那個呼叫會失敗,
  使用者看到的是「Inkling 突然整個不見了」。
- 換筆記資料夾之後,舊的 repository 不會再被還活著的舊頁面重新掛上一個沒有人釋放的
  資料夾監看器(換幾次資料夾就漏幾個,每個都還盯著舊目錄發事件)。
- **「記下並預覽」存檔失敗後,按「回上一步」再按一次 Enter 現在真的會重試。** 以前那條
  唯一的重試路徑是死的:退回去拿到的是同一個已經標記「存過了」的頁面實例,再按只是把
  同一則錯誤訊息重畫一遍,連寫檔都沒有嘗試。順帶修掉重試成功之後底部按鈕還寫著
  「回上一步」卻收掉整個面板。
- **「記下並預覽」頁補回「開啟檔案位置」(`Ctrl+L`)**,與清單頁、預覽頁一致 ——
  同一則筆記的三個畫面手勢本來就該通用。
- **編輯表單補上 `Ctrl+O`**(跳到預設編輯器),以前那一頁按這個鍵完全沒反應,
  而同一個動作坐在 `Enter` 上 —— 焦點在單行標題欄時很容易誤觸,卡片上未儲存的修改
  會靜靜消失。副標現在把這個代價講出來。
- **對沒有內文的筆記按 `Ctrl+Shift+C`,在預覽頁與記下並預覽頁不再完全靜默。**
  清單頁本來就會在那一列打一個「沒有內文」的標籤,那兩頁則是一點反應都沒有,
  跟快速鍵壞掉分不出來。
- **開頭就是一條 `---` 水平線的外來 Markdown,前半段內容不再消失。** Markdown 的水平線
  跟 YAML front matter 的分隔線是同一串字,而「第一行是水平線、後面某處還有一條」的
  文件並不罕見 —— 以前那中間的內容會被當成 front matter 吞掉,清單與預覽都看不到,
  而且在 Inkling 裡編輯一次之後那幾行會被寫進 front matter 區塊裡,它們沒有冒號,
  Obsidian / Hugo 從此解析不了那個檔案。現在區塊裡沒有任何一行是 `key: value` 就
  整檔當內文(跟「有開頭沒收尾」同樣處理)。
- **標題寫成 YAML 折疊純量(`title: >` / `title: |`)的筆記,標題不再變成一個 `>`。**
  續行以前會掉進「不認得的欄位」那一區,而那一區是寫在固定欄位後面的 ——
  於是那幾行縮排排到 `updated:` 底下,把日期變成多行純量、整份 front matter 就此壞掉。
  現在會把續行併成一行還給標題。
- **標題剛好以引號開頭又以引號結尾時,不再被剝掉一層。** 例如 `"賣點" 與 "痛點"`
  會變成 `賣點" 與 "痛點`,而下一次存檔又整個包起來,每編輯一輪就多一層殘骸。
- **存一則筆記不再讓清單重建兩遍。** Inkling 自己寫的檔案會被自己的資料夾監看器
  再通知一次,第二次還晚 250 毫秒(去抖動)才到,畫面因此多閃一下。現在自己剛寫過的
  那個路徑在短時間內的事件會被當成回音忽略;別的檔案、以及過了那段時間的同一個檔案,
  外部異動照樣即時反映。
- 同一秒存下的多則筆記在清單裡的順序現在是確定的(`updated` 只到秒,而原本用的是
  不穩定排序 —— 同一份資料夾在不同機器上可能排出不同順序)。
- 快速記下與編輯存檔後,回傳的內文換行與檔案裡的一致(以前記憶體裡那一份可能還帶著
  CRLF 或裸 CR,跟從磁碟讀回來的同一則比較起來不相等)。
- 換檔失敗時不再於筆記資料夾裡留下一個沒有人會提到的 `.md.tmp`(掃描只看 `*.md`)。
- 筆記資料夾裡有無權限讀取的子目錄時,清單不再整個打不開;讀不到的項目會跳過,
  無法讀取的資料夾會顯示看得懂的錯誤訊息,而不是擴展層級的失敗。
- 筆記資料夾被刪除再重建之後,外部異動會重新開始反映到清單(之前檔案監看器會永久失效)。
- 外來 Markdown 檔取標題與摘要時會跳過程式碼圍欄,不再把程式碼當成內文;
  標題與摘要相同時也不再重複顯示。
- 清單頁搜尋無結果時,不再錯誤地顯示「還沒有任何筆記」。
- 「記下並預覽」頁複製內文不再導致整個 Command Palette 面板消失。
- 快速記下存檔失敗時,不再連使用者打的內容一起弄丟。
- 設定頁的筆記資料夾欄位現在會擋下相對路徑與無效輸入,不再靜靜建立不存在的資料夾。
- 設定頁「瀏覽…」按鈕開啟失敗時會給予回饋,不再看起來像壞掉。
- 含逗號的標籤寫回檔案時會正確加引號;外部編輯器寫的 quoted 逗號標籤讀進來也不再裂開。
- 標題等含換行的 front matter 值不再產生讀不回來的格式。
- 繁體中文介面用詞統一為台灣 Windows 慣用語(「搜尋」「選取」)。
- 變更筆記資料夾等會釋放檔案監看的操作,不再因為內部計時器的釋放競態讓擴展進程崩掉。
- Command Palette 的結果裡不再多出一列點了沒反應的 **Inkling**。那是 Windows 的
  應用程式清單項被內建的應用程式搜索列了進來,而套件的 exe 是純 COM server,
  使用者「啟動」它本來就不會有任何事發生;現在套件不再登記到開始功能表
  (`AppListEntry="none"`)。解安裝與擴展載入都不受影響。

### 變更

- 套件中繼資料的 Description 改為英文(對應 Store / WinGet / gallery 的需求);
  這不是身分欄位,不影響已安裝使用者的設定。
- 失敗時的錯誤訊息統一補上使用者可以採取的下一步。
- 快速記下頁的圖示改成燈泡,不再進了頁面就換成加號(跟「新增筆記」的加號分開)。

### 新增(專案治理)

- MIT 授權(LICENSE)、SECURITY.md、issue 範本、CI 與 release 工作流程。
- 文檔拆成三份:`README.md`(使用者:怎麼用、有哪些鍵、筆記檔長什麼樣)、
  `docs/development.md`(建置、部署、專案結構、排錯)、`docs/design-notes.md`(設計考證)。
  README 另補上英文摘要與三張截圖。
- `docs/release-checklist.md` 與 `docs/gallery/`(gallery 投稿草稿;
  投稿用的 256×256 圖示在 `assets/gallery/icon.png` —— 它是 `tools/render-icons.ps1`
  的產出,跟其他產生出來的圖放在一起,理由見 `docs/gallery/README.md`)。

### 開發工具

- **發版流程修好了一個一定會炸的地方**:`release.yml` 的 release job 沒有 checkout,
  而 `gh` 解析 repo 的環境變數**只有 `GH_REPO`**——它不會回落到 Actions 內建的
  `GITHUB_REPOSITORY`,也沒有 git remote 可問。也就是第一次打 tag 發版時,建置、簽章、
  上傳全部成功之後最後一步會死在「fatal: not a git repository」,而 tag 已經推上去了。
  現在把 `GH_REPO` 傳進去。同一輪還修了三件:`makeappx bundle` 補上 `/bv`
  (不指定的話 bundle 版本會變成打包當下的日期時間,跟 `.msix` 裡的版本對不上)、
  **bundle 自己也要簽**(簽章不會從裡面的 `.msix` 傳遞到外層,只簽裡面的話側載一樣被擋)、
  發版前先跑一次 `dotnet test`(tag 可能打在沒過 CI 的 commit 上)。
- **兩個 workflow 的 `permissions` 收緊**:預設改成 `contents: read`,
  寫入權限只給真的要建 Release 的那個 job。
- `tools/cmdpal-ui.ps1` **不再照視窗標題找 CmdPal 的視窗**。標題跟著 Windows 顯示語言走,
  而腳本比對的是寫死的 `Command Palette` / `Command Palette Toast`,旁邊還註著
  「在 zh-TW 機器上實測仍是英文」—— 那句話在某次 CmdPal 進程重啟之後就不成立了
  (那一次連 CmdPal 自己的介面都從英文變成中文,變的是整個 app 的語言解析,不只是標題),
  於是整支腳本找不到面板、四輪重試全部失敗。改用結構特徵:
  `WinUIDesktopWin32WindowClass` 加 `WS_EX_TOOLWINDOW` 挑出「面板與 toast」
  (設定視窗有工作列按鈕、不帶那個旗標,所以不會被誤認),再用 `WS_DISABLED` 分辨兩者
  —— toast 不收輸入所以是 disabled。三條判準的實測數字寫在 `Get-CmdPalUiWindows` 的註解裡。
  另外真的找不到面板時會把當時看到的每一個視窗連同樣式印出來:「判準過時」跟
  「CmdPal 真的沒開」原本的訊息一模一樣,查起來會直接往錯的方向走。
- `tools/cmdpal-ui.ps1` 的 `state` 動作**修好了永遠印出空 alias 清單**的問題。
  它用 `Inkling*` 去比對命令 Id,而那些 Id 刻意還是改名前的 `Notelet.*`
  (CmdPal 的 alias 以純命令 Id 當鍵,改了會清掉使用者設過的 alias)——
  改名時漏改了這一處。空清單跟「真的沒設過 alias」長得一模一樣,是最糟的一種失效:
  看起來有在驗,實際上什麼都沒看到。現在連 CmdPal 的 alias 總數一起印,零命中時也明講一句。
- `tools/cmdpal-ui.ps1` **兩個原本被吞掉的截圖失敗現在會講話**。DPI 感知設不起來時,
  螢幕縮放不是 100% 的機器上 `shot` 會缺右邊與下面一塊,而那張圖乍看完全正常 ——
  現在啟動就警告,而且判準是**實際生效的等級**而不是那兩個 Set 函式的回傳值
  (「已經設過了」也會回 false,光看回傳值分不出「設失敗」與「本來就是了」)。
  `PrintWindow` 失敗時點陣圖是全黑的、`Save` 照樣成功,以前照樣印「已存」,
  現在明講那張是空的、不要拿它當證據。
- `tools/cmdpal-ui.ps1` 的「面板不能用」**不再一律說成「CmdPal 不在前景」**。
  那個判斷綁著三件事(前景是不是 CmdPal、面板在不在、UIA 讀不讀得到),而編輯頁的
  `Enter` 會開外部編輯器並收掉面板 —— 於是訊息說「不在前景」,底下卻印著前景就是
  `Microsoft.CmdPal.UI`,訊息跟它自己附的證據互相打臉。現在分三種講。
  同一批補上:預期結果本來就是「面板收起來」的驗證要帶 `-Retries 1`
  (否則整串會跑滿四次,那個開外部程式的動作也就做了四次)、`-MaxText` 的說明,
  以及「`|` 是唯一的步驟分隔符,沒有轉義,任何參數裡都不能出現它」。
- `tools/cmdpal-ui.ps1` 會送按鍵的動作改成在**送出之前**確認 CmdPal 的面板真的在前景
  且可以用了。原本是先 `SendInput` 再檢查 —— 而 `SendInput` 送到的永遠是當下的前景
  視窗,所以 CmdPal 不在前景時那串字會打進使用者正在用的別的視窗,檢查只是事後報告;
  失焦又會整串重跑最多 4 次,同一串字因此會被打進錯的地方好幾遍。
  「面板在不在」的判準改用 UIA 讀不讀得到子節點:面板收起來的那一小段時間裡
  視窗仍然 `IsWindowVisible`、前景進程也還是 CmdPal,只看視窗分辨不出來。
  另外 `show` 現在會確認熱鍵真的生效(以前只睡 900ms 就往下走,這是「字跑到別的
  地方」最常見的一條路),而且面板已經開著時不再送熱鍵 —— 那是 toggle,會把它關掉。
  全部重試都失敗時以非零結束,不再靜靜地 exit 0。
- `tools/cmdpal-ui.ps1` 宣告 per-monitor DPI 感知。螢幕縮放不是 100% 時,
  `GetWindowRect` 回報的是縮放後的邏輯座標而 `PrintWindow` 畫的是實體像素 ——
  截出來的圖會缺右邊與下面一整片,而且不會報錯。
- `tools/cmdpal-ui.ps1` 的 package family name 改為動態取得;遇到不認得的動作或按鍵
  會中止整串並以非零結束(原本印個警告繼續跑,後面的步驟會落在沒預期的地方)。
- `src/Inkling/Inkling.csproj` 的 `PublishSingleFile` 加上 RuntimeIdentifier 條件 ——
  官方模板那行無條件的 `true` 會讓方案層級的 `dotnet build` 報一個完全指不到問題的
  NETSDK1097。(方案層級仍然建不了擴展:MSIX 打包目標不吃 AnyCPU,那是打包專案的本質。)
