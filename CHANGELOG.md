# Changelog

本檔案遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 的格式。

## [Unreleased]

首次公開發佈前的修正輪(內部審查,尚未發版)。

### 新增

- **瀏覽筆記時按 `Ctrl+N` 直接開新增筆記的表單**,不必先退回主搜尋框。
  它出現在每一則筆記的 `Ctrl+K` 選單裡(排在「開啟檔案位置」與「刪除」之間)——
  CmdPal 的快速鍵只認當下選中項的命令,頁面層級沒有掛鍵的地方。
- **`Ctrl+U` 現在在預覽頁也能用**,而且切的是整頁內容:原文走 CmdPal 的純文字檢視器
  (`PlainTextContent`),等寬字、**縮排與連續空行一個字元都不動**,右鍵還能切自動換行與縮放。
  這解掉了「內文是 HTML / SVG 的筆記,預覽頁看起來完全空白」—— 那些標記會被 Markdown
  渲染器整段吃掉,內容其實一直都在。
- **原始文字模式改成全域狀態,而且會記住**:在預覽頁切掉,回清單頁的詳細窗格也是原文;
  關掉 CmdPal、重載擴展都還在(存在 `settings.json` 的 `Inkling.ShowSource`)。
  刪除筆記那一頁的詳細窗格也跟著走。設定頁上刻意沒有這一項 —— 切換鍵就是它的介面。

### 變更

- **`Ctrl+Enter` 在三個顯示筆記的畫面上統一成「編輯」**(清單頁、預覽頁、記下並預覽頁)。
  那顆按鈕是**位置鍵** —— 底部工具列的次要命令,坐上去的是誰只看命令排序,跟命令自己綁的
  快速鍵無關,所以以前預覽頁的 `Ctrl+Enter` 順位落在「複製內文」上,跟另外兩頁不同義。
  代價講明白:**預覽頁的 `Enter` 從「編輯」變成「完成(收起面板)」**(跟記下並預覽頁同形),
  複製內文只剩自己的 `Ctrl+Shift+C`。編輯照樣有 `Ctrl+E`。
  `Inkling：刪除筆記` 那一頁刻意不跟(它的兩個鍵是「先問一次」與「直接刪」)。

### 重新命名:Notelet → Inkling

專案改名了。趁尚未發版一次改到底 —— 套件身分、namespace、資料夾、腳本、文檔全部跟著換。

- 換名字的原因是搜尋:在 Command Palette 打 `note` 時 Notelet 會排在 Notepad 前面,
  等於這個擴展在擋使用者要開的東西。`ink` 這一帶乾淨得多。
- **`%LOCALAPPDATA%\Packages\<PFN>\LocalState` 換了位置**(PFN 從
  `Notelet_bf0n0751x5hse` 變成 `Inkling_bf0n0751x5hse`),舊的 `settings.json` 成為孤兒 ——
  筆記資料夾、快速記下分隔符、記下後預覽這三項會回到預設值,重設一次即可。
- **預設筆記資料夾也跟著改名**,從 `<OneDrive>\Notelet` 變成 `<OneDrive>\Inkling`。
  升級前用過預設資料夾的話,**把舊資料夾改名,或在設定裡把路徑指回舊的** ——
  否則筆記檔案都還在,清單卻會是空的。
- **Command Palette 端設過的 alias 不受影響。** CmdPal 的 `Aliases` 是以純命令 Id 當鍵,
  而那六個 Id 刻意保留原字串(仍是 `Notelet.*`)。釘選與擴展層級的啟用狀態會回到預設。
- MSIX 靠套件身分認「同一個套件的新版本」,所以舊的 Notelet 不會被取代。
  用 `Get-AppxPackage Notelet | Remove-AppxPackage` 移掉,否則兩個並存。

### 變更(圖示)

- 全新的圖示:一道有壓感的下筆加一顆句點,取自 inkling 的字面。
  刻意避開捲角便條紙 / 鉛筆加紙 / 記事本 —— 那是 Notepad、Sticky Notes、OneNote 的符號。
- **四個頂層命令改用自訂的單色圖示**(清單 / 快速記下 / 新增筆記 / 刪除筆記),
  同一個家族,右下角換修飾符。`Ctrl+K` 選單與頁面內維持 Segoe Fluent 字形。
- 自訂圖示每個備了淺色與深色主題兩張 PNG —— 字形會自動跟主題變色,PNG 不會。
- 代價講明白:刪除的圖示變成「筆畫＋叉」,不如垃圾桶一望即知。這是為了家族一致
  刻意付的;改回 `Glyph(0xE74D)` 只是一行的事。
- 套件圖示補齊 `Square44x44Logo` 的兩條候選階梯(`scale-100/125/150/200/400` 與
  `targetsize-16/24/32/48/256_altform-unplated`)。模板只給了 88px 與 24px 兩張,
  而要 unplated 的地方只有 24px 可挑 —— 在 150% DPI 下被放大,看起來是糊的。

### 修正

- 筆記資料夾裡有無權限讀取的子目錄時,清單不再整個打不開;讀不到的項目會跳過,
  無法讀取的資料夾會顯示看得懂的錯誤訊息,而不是擴展層級的失敗。
- 筆記資料夾被刪除再重建之後,外部異動會重新開始反映到清單(之前檔案監看器會永久失效)。
- 外來 Markdown 檔取標題與摘要時會跳過程式碼圍欄,不再把程式碼當成內文;
  標題與摘要相同時也不再重複顯示。
- 清單頁搜尋無結果時,不再錯誤地顯示「還沒有任何筆記」。
- 「記下並預覽」頁複製內文不再導致整個 Command Palette 面板消失。
- 快速記下存檔失敗時,不再連使用者打的內容一起弄丟。
- 設定頁的筆記資料夾欄位現在會擋下相對路徑與無效輸入,不再靜靜建立不存在的資料夾。
- 設定頁「瀏覽…」按鈕開啟失敗時會給予回饋,不再看起來像壞掉。
- 含逗號的標籤寫回檔案時會正確加引號;外部編輯器寫的 quoted 逗號標籤讀進來也不再裂開。
- 標題等含換行的 front matter 值不再產生讀不回來的格式。
- 繁體中文介面用詞統一為台灣 Windows 慣用語(「搜尋」「選取」)。
- 變更筆記資料夾等會釋放檔案監看的操作,不再因為內部計時器的釋放競態讓擴展進程崩掉。
- Command Palette 的結果裡不再多出一列點了沒反應的 **Inkling**。那是 Windows 的
  應用程式清單項被內建的應用程式搜索列了進來,而套件的 exe 是純 COM server,
  使用者「啟動」它本來就不會有任何事發生;現在套件不再登記到開始功能表
  (`AppListEntry="none"`)。解安裝與擴展載入都不受影響。

### 變更

- 套件中繼資料的 Description 改為英文(對應 Store / WinGet / gallery 的需求);
  這不是身分欄位,不影響已安裝使用者的設定。
- 失敗時的錯誤訊息統一補上使用者可以採取的下一步。
- 快速記下頁的圖示改成燈泡,不再進了頁面就換成加號(跟「新增筆記」的加號分開)。

### 新增(專案治理)

- MIT 授權(LICENSE)、SECURITY.md、issue 範本、CI 與 release 工作流程。
- 文檔拆成三份:`README.md`(使用者:怎麼用、有哪些鍵、筆記檔長什麼樣)、
  `docs/development.md`(建置、部署、專案結構、排錯)、`docs/design-notes.md`(設計考證)。
  README 另補上英文摘要與三張截圖。
- `docs/release-checklist.md` 與 `docs/gallery/`(gallery 投稿草稿與 256×256 圖示)。

### 開發工具

- `tools/cmdpal-ui.ps1` 會送按鍵的動作改成在**送出之前**確認 CmdPal 的面板真的在前景
  且可以用了。原本是先 `SendInput` 再檢查 —— 而 `SendInput` 送到的永遠是當下的前景
  視窗,所以 CmdPal 不在前景時那串字會打進使用者正在用的別的視窗,檢查只是事後報告;
  失焦又會整串重跑最多 4 次,同一串字因此會被打進錯的地方好幾遍。
  「面板在不在」的判準改用 UIA 讀不讀得到子節點:面板收起來的那一小段時間裡
  視窗仍然 `IsWindowVisible`、前景進程也還是 CmdPal,只看視窗分辨不出來。
  另外 `show` 現在會確認熱鍵真的生效(以前只睡 900ms 就往下走,這是「字跑到別的
  地方」最常見的一條路),而且面板已經開著時不再送熱鍵 —— 那是 toggle,會把它關掉。
  全部重試都失敗時以非零結束,不再靜靜地 exit 0。
- `tools/cmdpal-ui.ps1` 宣告 per-monitor DPI 感知。螢幕縮放不是 100% 時,
  `GetWindowRect` 回報的是縮放後的邏輯座標而 `PrintWindow` 畫的是實體像素 ——
  截出來的圖會缺右邊與下面一整片,而且不會報錯。
- `tools/cmdpal-ui.ps1` 的 package family name 改為動態取得;遇到不認得的動作或按鍵
  會中止整串並以非零結束(原本印個警告繼續跑,後面的步驟會落在沒預期的地方)。
- `src/Inkling/Inkling.csproj` 的 `PublishSingleFile` 加上 RuntimeIdentifier 條件 ——
  官方模板那行無條件的 `true` 會讓方案層級的 `dotnet build` 報一個完全指不到問題的
  NETSDK1097。(方案層級仍然建不了擴展:MSIX 打包目標不吃 AnyCPU,那是打包專案的本質。)
