# 手動驗證清單

## 為什麼需要這份清單

Command Palette 沒有提供 UI 自動化介面,擴展又跑在獨立的 COM 進程裡,沒有辦法自動驅動
它的畫面。所以驗證分成兩半:

| | 怎麼驗 |
|---|---|
| 儲存、解析、搜索、標題/內文切分 | `dotnet test` —— 全自動,涵蓋所有非 UI 邏輯 |
| 套件有沒有真的裝上 | `tools/VerifyRegistration` 探針 —— 全自動,查 Windows 的 AppExtension 目錄 |
| 存檔結果對不對 | 直接看 `%OneDrive%\Notelet` 底下的檔案內容,可以斷言 |
| 清單內容、快速鍵、placeholder、有沒有跳 toast | `tools\cmdpal-ui.ps1` —— 半自動,驅動 Windows 的 UI Automation |
| **顏色、圖示長什麼樣、游標落在哪** | **只能靠眼睛,就是這份清單** |

換句話說:「行為對不對」靠測試,「畫面上有什麼」可以用 `cmdpal-ui.ps1` 讀出來斷言,
「長得對不對」才需要人。

`cmdpal-ui.ps1` 的用法與它讀不到的東西寫在 `.claude/skills/verify-cmdpal-ui/SKILL.md`。
最常用的兩條:

```powershell
pwsh -NoProfile -File tools\cmdpal-ui.ps1 -Steps "notes"    # 先確認資料夾不是真資料
pwsh -NoProfile -File tools\cmdpal-ui.ps1 -Steps "show|type:# |wait:1400|tree:6"
```

**跑會寫入的項目之前先確認筆記資料夾。** 它預設指向使用者真正在用的
`%OneDrive%\Notelet` —— 新增與刪除類的驗證要先把資料夾換成暫存目錄,跑完換回去。

## 每次改動後的前置動作

```powershell
.\tools\deploy.ps1 -Configuration Release -Reload
```

`-Reload` 需要先打開 CmdPal 設定 → 一般 → For developers → Enable external reload;
沒開的話腳本會提醒你,改成在 Command Palette 手動執行 **Reload**(要選副標題是
`Reload Command Palette extensions` 的那一個)。沒跑 Reload 的話 CmdPal 會繼續用舊的
擴展實例,你會以為改動沒生效。

設定頁的擴展清單出現**兩個 Notelet** 的話,再 Reload 一次就會收回一個 —— 那是 CmdPal
在套件重新註冊時多建了一個 provider,不是擴展壞掉,不必重開 PowerToys。

**每次 Reload 或重新部署之後,設定頁要關掉重開**(退回 Extensions 清單再點進來)。
那個頁面綁在舊的擴展實例上,不重開的話按 Save 會靜靜地什麼都不做 —— 驗設定相關的項目時
先做這一步,否則你驗到的是一顆死按鈕。

---

## 1. 擴展載入

- [ ] 叫出 Command Palette,往下捲(或按一次 `↑`)能看到 **Notelet**
- [ ] 也能看到 **Notelet:快速記下**、**Notelet:新增筆記** 與 **Notelet:刪除筆記**
- [ ] 圖示正常顯示,不是空白方塊

Reload 之後跑這段,確認 CmdPal 載入的是你剛部署的那一份 —— 不是某個舊佈局:

```powershell
(Get-AppxPackage -Name Notelet).InstallLocation
(Get-Process Notelet -ErrorAction SilentlyContinue).Path
```

兩者應該指向同一個資料夾。第二行沒有輸出代表 CmdPal 還沒把擴展的 COM server 拉起來,
先進一次 Notelet 頁面再看。

設好 alias 之後跑這段,確認 CmdPal 記的是我們寫死的 Id,而不是它自己拿標題算出來的雜湊
—— 這是快速記下唯一入口的生死線:

```powershell
$s = Get-Content "$env:LOCALAPPDATA\Packages\Microsoft.CommandPalette_8wekyb3d8bbwe\LocalState\settings.json" -Raw | ConvertFrom-Json
$s.Aliases.PSObject.Properties | Where-Object { $_.Value.CommandId -like 'Notelet*' } | ForEach-Object { "$($_.Name) -> $($_.Value.CommandId)" }
```

- [ ] 印出來的 CommandId 是 `Notelet.QuickCapturePage`,
      而不是 `Notelet_…!App!Notelet` 後面接一串數字

## 2. 快速記下(需求 1)

先設好入口:設定 → Extensions → Notelet → `Notelet:快速記下` → Alias 填 `n`。

- [ ] 主搜尋框打 `n` 空白 —— 搜尋框被清空,**直接進到快速記下頁**
      (indirect alias 存的鍵是「alias + 空白」,所以是打完空白那一刻觸發)
- [ ] 頁面剛進去、還沒打字時,顯示的是「打字就記下」的提示,**沒有**「記下:」那一列
- [ ] 打 `測試想法一`,第一列出現 **記下:測試想法一**,分在「記下」那一節底下
- [ ] 按 Enter,跳出「已記下」的 toast,**而且不會留在快速記下頁**
      (toast 視窗會搶焦點,CmdPal 主視窗一失焦就把自己藏起來,所以看起來是整個收掉。
      要改成「存完停在筆記上」見 2c)
- [ ] `%OneDrive%\Notelet` 底下出現 `<日期>-<時間>-測試想法一.md`
- [ ] 打開該檔案,front matter 的 `id` / `title` / `created` / `updated` 都在,內文是空的
- [ ] **沒有 `tags:` 那一行**(空的標籤不寫進檔案 —— 手機的雲端硬碟 App 不渲染
      Markdown,front matter 是最先看到的東西,沒功能的欄位不佔那一行)
- [ ] **不吵人檢查**:主搜尋框打一般查詢(`notepad`、`note about x`、`chrome`),
      清單裡**不應該**出現任何 Notelet 的記下項目,也不應該有點不動的空列

相似筆記提醒(換成頁面才做得到的 —— fallback 只有一列):

- [ ] 再打一次 `n ` 然後輸入 `測試想法一`,「記下」那一列底下出現
      **已經記過的** 一節,列著剛才存的那則
- [ ] 按方向鍵選中既有的那則、Enter,進的是**預覽頁**而不是又存一則新的
- [ ] 剛存完一則之後立刻再打一樣的標題,新存的那則**馬上**出現在「已經記過的」裡
      (項目快取的鍵有帶 repository 的 Version,只以查詢字串為鍵的話這裡會看到舊結果)
- [ ] 打一個完全沒記過的標題,只有「記下」一列,不會有「已經記過的」那一節

分隔符切開標題與內文(以下都在快速記下頁裡打,分隔符維持預設的 `;;`):

- [ ] 頁面的 placeholder 寫的是 **打字記下想法,`;;` 後面接內文…**
- [ ] 還沒打字時那塊提示的副標是 **「買咖啡機;;比較過幾台」…**(例子用的是**真的**分隔符,
      不是單一個分號 —— 單一個分號根本不會切)
- [ ] 打 `測試想法二;;這是內文`,項目標題是 **記下:測試想法二**,副標題是 **內文:這是內文**
- [ ] Enter 之後,檔案的 `title` 是「測試想法二」,內文是「這是內文」,檔名用的是標題
- [ ] **單一個分號不切**:打 `for (var i = 0; i < 10; i++)`,整句都是標題,沒有內文
- [ ] 全形 `；；`(中文輸入法直接打出來的那個)一樣有效
- [ ] 半形全形混著打(`;；` 或 `；;`)也有效 —— 中英切換時打出哪一個並不受控
- [ ] `標題;;第一段;;第二段` 只切第一組,內文是 `第一段;;第二段`
- [ ] `標題;;`(分隔符打了、內文還沒打)照樣可以存,存出來只有標題
- [ ] `;;只有內文` 不會出現「記下:」

### 2a. 換掉分隔符(設定 → 快速記下的分隔符)

**這一段的重點是「不必 Reload」**:CmdPal 握著的是使用者當下開著的那個頁面實例,
靠 provider 重建是換不掉它的(見 README〈標題與內文用分隔符切開〉)。

- [ ] **先把快速記下頁開著**,另外從 CmdPal 設定 → Extensions → Notelet 把分隔符改成 `,,`,存檔
- [ ] 回到那個**還開著的**快速記下頁(不要 Reload、不要重進):placeholder 已經變成
      **…`,,` 後面接內文…**,空白提示的例子也換成 `買咖啡機,,比較過幾台`
- [ ] 打 `測試想法三,,這是內文` → 切得開,標題是「測試想法三」
- [ ] 打 `測試想法四;;這是內文` → **整句都是標題**,`;;` 沒有留成備援
- [ ] 全形 `，，` 一樣切得開
- [ ] 設定填成全形 `，，`、打字打半形 `,,` → 照樣切得開(設定那一頭也走同一個折算)
- [ ] 把欄位**清空**存檔 → 回到預設 `;;`,而且設定頁再打開時輸入框顯示的就是 `;;`
      (存回去的是整理過的值,不會出現「看起來設了、其實沒生效」)
- [ ] 填一個前後帶空白的值(例如 `␣->␣`)→ 空白被去掉,`標題->內文` 切得開
- [ ] 填多字元的 `-->` → `標題-->內文` 切得開;`標題--`(只打了一半)整句都是標題
- [ ] 分隔符改完之後,頂層那一列的副標仍然是 **打字直接存成筆記,分隔符後面接內文**
      (那一列跟不上設定,所以刻意不提「分號」)
- [ ] 改回 `;;` 收尾

貼上多行內容(CmdPal 搜尋框是單行的,這條是繞過它的路):

- [ ] 複製一段**多行**的 Markdown(至少三行,含一個 `#` 標題與一個清單)
- [ ] 進快速記下頁打一個標題,清單出現**第二列**「記下:<標題>」,
      副標題是 **內文取自剪貼簿(N 行)**,行數與你複製的相符
- [ ] 選那一列按 Enter,打開檔案 —— 內文是**完整的多行原文**,換行與縮排都在
- [ ] 對照第一列(普通的「記下」):它存出來的內文只有你在搜尋框裡看得到的那些字
- [ ] 剪貼簿是**單行**文字時,那一列**不該出現**(單行搜尋框自己貼得進去)
- [ ] 剪貼簿是空的、或放的是圖片時,頁面正常運作,不跳錯誤
- [ ] **不該有任何前綴判斷**:打 `note about something` 照樣出現
      「記下:note about something」—— 進得了這一頁就代表意圖明確,不該再擋一次

全域快速鍵(可選,設了就連 `n` 都省):

- [ ] 給 `Notelet:快速記下` 設一個全域快速鍵,按下去直接進頁面,打字、Enter 一樣可用

### 2b. alias 不會失效(Id 的回歸測試)

快速記下唯一的入口就是 alias,而 alias 存的鍵是命令的 `Id`。改標題不該影響它:

- [ ] 設好 alias 之後 Reload 一次,`n ` **照樣**直接進快速記下頁
- [ ] 重新部署(`deploy.ps1`)之後 alias 也還在
      (曾經有 bug:命令沒有設 `Id`,CmdPal 就拿 `ProviderId + DisplayTitle + Title + Subtitle`
      算一個雜湊當 Id。標題變一個字,使用者設過的 alias / 快速鍵 / 釘選就全部對不上。
      修法是寫死 `Id`,見 `src/Notelet/CommandIds.cs`)

### 2c. 記下後先看一眼(設定 → 記下後先看一眼)

同一時間只有一條路在,設定決定是哪一條。詳細理由見 README
〈記下之後要不要先看一眼〉。

設定**開啟**時(預設):

- [ ] **先把快速記下頁開著**再去設定裡把它關掉又打開,回到那個**還開著的**頁面
      (不要 Reload、不要重進):打字之後按 Enter,走的是預覽那條路 ——
      項目快取的鍵有帶這個設定值,少了它會看到「設定改了、Enter 行為沒變」
- [ ] 存檔之後**停在一頁 Markdown 上**,看得到剛記下的標題與內文,
      **沒有** toast,Command Palette **沒有消失**
- [ ] 在那一頁按 Enter → 整個 Command Palette 收起來(不是回到主搜尋框)
- [ ] 預覽頁上 `Ctrl+E` 進編輯表單,改完存檔回到預覽頁,**內容跟著更新**
      (CmdPal 不會因為導覽回來就重新取內容,靠的是表單回呼)
- [ ] 預覽頁的選單裡「在預設編輯器開啟」與「複製內文」都在,而且指的是剛記下的那則
      (這幾個命令是存檔**之後**才補上去的,靠 `PropChanged` 讓 CmdPal 重讀 ——
      沒生效的話這一頁會只剩「完成」一顆)
- [ ] 「內文取自剪貼簿」那一列也吃這個設定:剪貼簿放多行文字,Enter 進預覽頁,
      看到的是**完整的多行內文**
- [ ] **只存一次**:預覽頁上按 `Ctrl+E` 進編輯再退回來、或讓頁面重新整理,
      資料夾裡**只有一個**檔案(存檔那一段有只跑一次的旗標,少了它會存成好幾則)
- [ ] 存檔失敗的路(可選,把筆記資料夾設成一個不存在的磁碟機代號如 `Z:\Notelet`):
      Enter 之後那一頁顯示 **存檔失敗** 與原因,底下還留著剛打的標題與內文,
      按 Enter 是**回上一步**(回快速記下頁,搜尋框裡的字還在),不是收起 Command Palette

設定**關閉**之後(從 CmdPal 設定 → Extensions → Notelet 取消勾選存檔):

- [ ] 快速記下頁打一個標題按 Enter:toast「已記下」+ Command Palette 消失
- [ ] `Ctrl+K` 打開選單,**只有**「記下」一項 —— 沒有另一個「先看一眼」的變體
      (做過又拿掉的東西,回歸的話選單會多一列)
- [ ] 驗完記得勾回來(預設是開的)

設定卡片本身:

- [ ] 設定頁上那一項是一個**核取方塊**,旁邊的字是「記下後先看一眼」,底下有一段淡色說明
      (Adaptive Cards 的 `Input.Toggle`;渲染不出來的話這一項會整個不見)
- [ ] 取消勾選按儲存,再打開設定頁 —— **還是沒勾**(反過來也驗一次)
- [ ] 勾著的狀態下只改資料夾按儲存 → 這個開關**沒有被還原**
      (卡片的值是建構時烤進 `DataJson` 的,`OnSettingsApplied` 一進來就 `Refresh()`
      正是為了這件事,見 README〈設定頁有兩個入口〉)

## 3. 瀏覽與搜索(需求 2)

先多記幾則不同內容的筆記,再驗:

- [ ] **新增後立刻回到清單,新筆記就在裡面**(曾經有 bug:清單頁的項目快取只以查詢字串
      為鍵,新增之後回來拿到的還是舊結果,畫面顯示「還沒有任何筆記」。
      修法是把 repository 的 `Version` 一起納入快取的鍵)
- [ ] 進入 Notelet,列出所有筆記,最新的在最上面
- [ ] 右側詳細窗格顯示該則筆記的內容
- [ ] 輸入標題裡的關鍵字,能找到
- [ ] 輸入**只出現在內文**的關鍵字,也能找到
- [ ] 輸入兩個關鍵字(空白隔開),只留下兩個都命中的
- [ ] 全形空白隔開的兩個關鍵字也有效(中文輸入法很容易打出全形空白)
- [ ] 大小寫不敏感(用英文筆記測 `docker` / `DOCKER`)
- [ ] 標題命中的排在只有內文命中的前面
- [ ] **手感檢查**:連續快速輸入,清單跟得上,沒有可感的延遲或卡頓
- [ ] 一則筆記都沒有時,顯示的是「還沒有任何筆記」的提示而不是空白
- [ ] 右側詳細窗格是**最寬**那一檔,清單與詳情大約各佔一半
      (寫死的,沒有設定項也沒有快速鍵。壞掉時的症狀是它變成最窄的 3:1 ——
      `ContentSize` 的 0 就是 `Small`,`Details.Size` 漏掉不會有編譯錯誤。
      理由見 README〈詳細面板寬度固定在最寬〉)

## 4. 原始文字切換(`Ctrl+U`)

在清單頁選中一則**有格式的**筆記(至少含一個 `#` 標題與一段 `**粗體**`):

- [ ] 按 `Ctrl+U`,右側窗格從渲染結果變成原始 Markdown,`#` 與 `**` 都看得到
- [ ] **選中的項目沒有跳掉**,還是原本那一則(這是這個功能的重點:實作是換掉
      `ListItem.Details` 而不是 `RaiseItemsChanged`,清單本身完全不動)
- [ ] **按下去就要變**,不需要離開再重進清單頁(曾經有 bug:改的是 `Details.Body`,
      而 `IDetails` 沒宣告成可觀察介面,那個通知跨不過 out-of-process 邊界,
      表現出來就是「值改了、畫面不動」)
- [ ] 再按一次 `Ctrl+U` 切回渲染結果
- [ ] `Ctrl+K` 打開選單,那一項的字會跟著狀態變(「顯示原始文字」↔「顯示渲染後的預覽」)
- [ ] 在原始文字模式下,用滑鼠拖曳選取窗格裡的一段文字並 `Ctrl+C`,貼出來與檔案內容一致
      (若 CmdPal 的 Markdown 控件不支援選取,記在下面的「記錄」裡 —— 那是控件的限制,
      不是這個功能沒生效)
- [ ] 切到原始文字模式後再打字搜索,清單換了但仍然停在原始文字模式
- [ ] 切換後上下移動選中項,其他筆記也是原始文字模式
- [ ] 原始文字模式**沒有外框、沒有底色**,字體與其他內容一致,跟著窗格寬度自然換行
- [ ] 內文**本身含有程式碼區塊**的筆記,在原始文字模式下整則都還是純文字,
      連 ``` 那三個反引號本身都看得到,不會有任何一段被渲染成別的樣子
- [ ] 沒有內文的筆記顯示「(沒有內文)」,兩個模式都不會出錯

## 4b. 複製內文與開啟檔案位置

在清單頁選中一則**有內文**的筆記:

- [ ] `Ctrl+K` 選單裡看得到 **複製內文**(`Ctrl+Shift+C`)、**在預設編輯器開啟**(`Ctrl+O`)、
      **開啟檔案位置**(`Ctrl+L`),每一項右邊都寫著自己的鍵位
- [ ] 按 `Ctrl+Shift+C` → **面板還開著、還停在清單頁,選中的還是同一列**,而且**沒有任何 toast**
      (這一條是重點:toast 會搶焦點,主視窗一失焦就自我隱藏,所以複製一次就會關一次面板)
- [ ] 同一列右邊出現一個 **`已複製`** 標籤,**約 2.5 秒後自己消失**
- [ ] 標籤還在的時候按方向鍵上下移動 → 清單不會跳掉、也不會重新整理
- [ ] 複製另一則 → 上一則的標籤消失,新的那則才有(同一時間只會有一個)
- [ ] 貼到別的地方 → 是**內文**,`---` 那幾行 front matter **不在裡面**,標題也不在
- [ ] **空內文不會清掉剪貼簿**:先複製一段別的文字,再選中一則**沒有內文**的筆記按
      `Ctrl+Shift+C` → 那一列的標籤寫 **`沒有內文`**,而且**剛剛複製的東西還貼得出來**
      (`ClipboardHelper.SetText` 會先 `EmptyClipboard()` 再寫入,所以這條是回歸測試)
- [ ] 按 `Ctrl+O` → 那個 `.md` 用系統預設的程式開起來
- [ ] 按 `Ctrl+L` → **檔案總管打開,而且那個 `.md` 是選中狀態**(不是只開資料夾)
- [ ] 對**放在子資料夾裡**的筆記按 `Ctrl+L` → 開的是那個子資料夾

進到預覽頁(在清單頁按 `Enter`)再驗一次 —— 同一則筆記在兩個畫面上要能用同一組手勢:

- [ ] `Ctrl+Shift+C` / `Ctrl+O` / `Ctrl+L` 都做同一件事
- [ ] 預覽頁按 `Ctrl+Shift+C` → **面板還開著、還停在預覽頁**,貼得出內文;
      這一頁**不會有標籤**(沒有清單列可以掛),是預期的
- [ ] `Ctrl+E` 進編輯表單
- [ ] `Ctrl+K` 選單裡**沒有刪除**,按 `Ctrl+D` 也什麼都不會發生
      (刪掉正在看的東西沒有道理)

## 5. Markdown 預覽(需求 3)

先建一則內文含各種語法的筆記(可以直接用編輯器寫進檔案):

```markdown
## 二級標題

- 清單項目一
- 清單項目二

| 欄位 | 值 |
|---|---|
| a | 1 |

`行內程式碼` 與 **粗體**

\`\`\`csharp
var x = 1;
\`\`\`

[連結](https://example.com)
```

- [ ] 選中該筆記按 Enter,進入預覽頁
- [ ] 標題渲染成 H1,而且**沒有重複出現兩次**
- [ ] 二級標題、清單、表格、程式碼區塊、粗體、連結都正確渲染
- [ ] **打三行看到三行**:內文打 `111` / `222` / `333` 三行,預覽顯示三行而不是
      `111 222 333`(標準 Markdown 會併成一行,Notelet 只在渲染時把單換行當成換行,
      磁碟上的檔案不變)
- [ ] 程式碼區塊裡的內容**沒有**被加上多餘的行尾空白(用「在預設編輯器開啟」對照原檔)
- [ ] 清單頁右側詳細窗格的換行行為跟預覽頁一致
- [ ] `Ctrl+K` 能看到「編輯」與「在預設編輯器開啟」
- [ ] 「在預設編輯器開啟」真的用系統預設程式打開了那個 `.md`
- [ ] 「複製內文」之後貼上,內容正確

## 6. 編輯(需求 4)

- [ ] 在清單選中一則筆記,`Ctrl+E` 進入編輯表單
- [ ] 表單帶出目前的標題與內文
- [ ] **欄位順序是「內文在上、標題在下」**,而且**游標一進來就在內文框**,不用按 Tab
      就能直接打字(順序就是為了游標:CmdPal 聚焦卡片裡第一個可聚焦的控件,
      Adaptive Cards 沒有 autofocus 也沒有 tabIndex)
- [ ] 卡片**底部**有一行淡色小字:**游標落在內文開頭。按 Ctrl+End 跳到最後接著寫。**
- [ ] 照著做:一進來直接按 `Ctrl+End`,游標跳到內文最後,可以接著往下寫
- [ ] **那行字沒有把焦點搶走** —— 加了它之後游標還是落在內文框
      (它是 `TextBlock` 不是 `Control`,`FindFirstFocusableElement` 不會選中它。
      游標的**位置**擴展控制不了,只能靠這行字告訴使用者,理由見 README「編輯表單」)
- [ ] 內文欄位是多行輸入框,不是單行
- [ ] 從內文按一次 Tab 到標題、再一次到「儲存」,按 Enter 能存檔(沒有 `Ctrl+S`,
      理由見 README「編輯表單」)
- [ ] 改完按「儲存」,跳出「已儲存」的 toast 並回到上一頁
- [ ] **回到預覽頁後,看到的是剛改完的內容**(曾經有 bug:CmdPal 不會因為導覽回上一頁
      就重新呼叫 `GetContent()`,畫面會停在存檔前的樣子。修法是存檔後由表單回呼預覽頁的
      `RaiseItemsChanged`)
- [ ] 從清單按 `Ctrl+E` 編輯並儲存,回到清單時標題與摘要也已更新
- [ ] 檔案內容確實更新,`updated` 時間戳有變,`id` 與 `created` **沒有**變
- [ ] **檔名沒有跟著標題改變**
- [ ] 把標題清空後儲存,會被擋下來並提示

**不吃掉別人的欄位**(這條最重要):

- [ ] 用文字編輯器在某則筆記的 front matter 手動加上:
      ```yaml
      cssclass: reading
      aliases:
        - 別名一
      ```
- [ ] 同一則筆記的 `tags` 也手動填上值(沒有那一行就自己加 `tags: [idea, 咖啡]`)
- [ ] 回到 Notelet 編輯這則筆記並儲存
- [ ] 重新打開檔案,`cssclass` 與 `aliases` **仍然在**
- [ ] `tags: [idea, 咖啡]` **也仍然在**(省略只針對空值,有值的標籤不能被吃掉)

## 7. 新增(完整表單)

- [ ] 執行 `Notelet:新增筆記`,出現空白表單
- [ ] **欄位順序是「標題在上、內文在下」**,而且**游標一進來就在標題框**
- [ ] **沒有** `Ctrl+End` 那行提示(新增時內文本來就是空的,游標在哪都一樣)
- [ ] **內文框一看就是五行高**,不是一行(這是預填空行撐出來的 —— 卡片沒有「幾行高」
      這種屬性)
- [ ] 按 Tab 進到內文框時整段預填內容被全選,打第一個字就直接覆蓋掉
- [ ] 填標題與多行內文並儲存,回到首頁並跳出 toast
- [ ] 檔案內容正確,內文的換行有保留
- [ ] **預填的空行沒有被存進檔案**:用滑鼠點進內文框中間(不要 Tab)、在第三行打幾個字
      再儲存,打開檔案應該只有那幾個字,前後不帶空行

## 7b. 清單頁的刪除(`Ctrl+D`)

先記兩則測試用的筆記,在清單頁選中其中一則:

- [ ] `Ctrl+K` 選單裡看得到 **刪除**,右邊寫著 `Ctrl+D`
- [ ] **那一列是紅的** —— 圖示、標題、右邊的鍵位三個都紅,選單裡其他項目維持原色
      (`CommandContextItem.IsCritical`。整列還是白的話先確認真的 Reload 過,
      再看 README〈刪除的紅色只有一個地方碰得到〉)
- [ ] 關掉選單,直接按 `Ctrl+D` → 一樣跳出確認框
- [ ] **搜尋框裡打幾個字,按 `Ctrl+Delete` → 刪掉游標右邊那個詞,不會跳出確認框**;
      按 `Delete` → 刪掉游標右邊一個字(那是文字框的標準編輯鍵,所以刪除才落在 `Ctrl+D`)
- [ ] **搜尋框的其他編輯鍵都還在**:`Ctrl+A` 全選、`Ctrl+C` 複製剛打的字、
      `Ctrl+Z` 復原 —— 一個都不該被擴展搶走
- [ ] 從選單點「刪除」,跳出確認框,寫著「刪除這則筆記?」與那則筆記的標題
- [ ] **確認框的兩顆按鈕都是預設樣式,沒有紅色**(那裡碰不到顏色,見上面那節 README)
- [ ] **焦點落在「刪除」上,Enter 直接確認**(0.11 的 CmdPal 從不設 `DefaultButton`,
      設不設 `IsPrimaryCommandCritical` 都一樣;那個旗標要等 CmdPal 更新才會生效,
      見 README〈確認框的按鈕沒有顏色,也沒有「危險」樣式〉)
- [ ] **選「取消」→ 筆記還在**,檔案也還在
- [ ] 再走一次選單並確認 → **沒有任何 toast**
- [ ] **清單立刻少一則**,不必離開再重進(repository 的 `Changed` 會通知)
- [ ] **面板還開著,而且留在清單頁** —— 這一條是重點。以前這裡會發一個
      「已移到資源回收筒」的 toast,而 toast 一搶焦點主視窗就自我隱藏,
      刪一則等於把整個面板關掉一次(見 README〈刪除成功時一個 toast 都不發〉)
- [ ] `%OneDrive%\Notelet` 底下那個 `.md` 不見了
- [ ] **打開 Windows 資源回收筒 —— 那個檔案在裡面**,而且能還原
      (這是刪除功能唯一不可回頭的部分,務必實際確認一次。實作走的是
      `SHFileOperationW` 的 `FOF_ALLOWUNDO`,少了那個旗標就是永久刪除)
- [ ] 還原之後回到 Notelet,那則筆記重新出現在清單裡
- [ ] 另一則沒被選中的筆記完全沒受影響

### 7c. 刪除筆記(是一頁,不是一個動作)

**這一節會清空資料夾。先確定筆記資料夾指向的是測試用的地方。**

前置:記三則筆記,再用文字編輯器在**子資料夾**裡放一個沒有 front matter 的
`外來的.md`,另外放一個 `readme.txt`。

先看那一頁本身 —— 打開它不該刪掉任何東西:

- [ ] 執行 `Notelet:刪除筆記`,**進到一個清單頁,什麼都沒被刪**
- [ ] 頂層那一列的副標是「挑幾則刪掉,或整個清空」,動作標籤是「開啟」
- [ ] 第一區「動作」有兩列:**刪除全部 4 則**(副標是筆記資料夾路徑)與
      **只刪 Notelet 建立的 3 則**(副標「保留 1 則不是 Notelet 建立的」)
- [ ] 下一區「不是 Notelet 建立的」排在 Notelet 筆記**前面**,圖示不一樣(警告),
      副標是相對路徑,看得出它在子資料夾裡
- [ ] 選中動作那兩列時,詳細窗格寫著資料夾路徑、總則數、外來幾則、以及資源回收筒那段
- [ ] 選中任何一則筆記,詳細窗格是它的內容
- [ ] `Ctrl+K` 選單裡第二項是 **預覽**,點進去是唯讀的渲染結果,**沒有刪掉東西**
- [ ] `readme.txt` **不在清單裡**(掃描只吃 `.md`)

兩個鍵位(這一節的重點):

- [ ] 選中一則**自己的**筆記,底部工具列寫著 **刪除 ⏎** 與 **直接刪除 Ctrl+⏎**
- [ ] 按 `Enter` → **跳確認框**,寫著「刪除這則筆記?」與那則的標題
- [ ] 取消 → 筆記還在
- [ ] 再按 `Enter` 並確認 → 那一列當場消失,**沒有 toast,面板還開著**
- [ ] 選中另一則自己的筆記按 `Ctrl+Enter` → **不跳確認框,直接刪掉**,面板還開著
- [ ] 在同一列按 `Ctrl+D`(清單頁的刪除鍵)→ **什麼都不會發生**。這一頁刻意不綁它:
      `Enter` 與 `Ctrl+Enter` 本來就是刪除,而且語意會打架 —— 清單頁那個鍵是「先問一次」,
      這一頁的次要命令是「不問」
- [ ] `Ctrl+K` 選單裡的 **直接刪除**(外來檔案那一列是 **刪除**)**是紅的**
- [ ] **底部工具列的「刪除 ⏎」與「直接刪除 Ctrl+⏎」還是原色**,不是紅的 ——
      同一個命令在選單裡紅、在工具列上不紅,那是 CmdPal 的兩個樣板,不是漏設
      (見 README〈刪除的紅色只有一個地方碰得到〉)
- [ ] 動作區「刪除全部 N 則」那一列的圖示**也還是原色**(清單列沒有 `IsCritical`)
- [ ] **連著按 `Ctrl+Enter` 刪掉好幾則** —— 每次刪完面板都還在;
      焦點跳到動作區第一列(「刪除全部」)時再按 `Ctrl+Enter`,**什麼都不會發生**
      (那一列沒有次要命令,這正是連續刪要用 `Ctrl+Enter` 而不是 `Enter` 的理由,
      見 README〈「刪除全部」排第一的代價〉)
- [ ] 反過來驗一次:焦點停在「刪除全部」那一列時按 `Enter` → 跳的是
      **「刪除全部 N 則筆記?」** 的確認框,取消就沒事

外來檔案不給跳過確認:

- [ ] 選中那則**外來的** `.md`,`Ctrl+K` 選單裡第一項寫的是 **刪除**(不是「直接刪除」),
      副標是「不是 Notelet 建立的,一律先確認」
- [ ] 按 `Ctrl+Enter` → **一樣跳確認框**,寫著「刪除這個檔案?」與「不是 Notelet 建立的」
- [ ] 按 `Enter` → 同一個確認框
- [ ] 取消 → 檔案還在;確認 → 檔案進資源回收筒,面板還開著

只刪自己建的:

- [ ] 按「只刪 Notelet 建立的 3 則」→ 確認框寫著「刪除 Notelet 建立的 3 則筆記?」,
      說明提到另外 1 則不會動到
- [ ] **焦點一樣落在主要按鈕上**(批次刪除有設 `IsPrimaryCommandCritical`,但 0.11 的
      CmdPal 不理它,所以看起來跟單則刪除一樣 —— 哪天焦點變成落在「取消」,
      表示 CmdPal 更新上來了,回頭把這一行跟 README 那節一起改)
- [ ] 取消 → 四則都還在
- [ ] 再按一次並確認 → **沒有 toast**
- [ ] **面板還開著、留在這一頁**,而且清單當場刷新成只剩那則外來檔案
      (這條走的是 `CommandResult.KeepOpen()`,而且是從確認框回來的路徑 ——
      要是實際上跳回了主畫面,就把它改成 `GoHome()` 並更新這一行)
- [ ] 只剩外來檔案時,「只刪 Notelet 建立的」那一列**消失了**(刪了等於什麼都不做)
- [ ] 那個 `.txt` 還在

全部刪掉:

- [ ] 補回三則筆記,選中**第一列**「刪除全部 4 則」→ 確認框標題是「刪除全部 4 則筆記?」,
      說明裡有**資料夾路徑**與「含子資料夾」,並提到其中 1 則不是 Notelet 建立的
- [ ] 確認 → **沒有 toast**,頁面當場變成 **「沒有筆記可以刪除」**,副標是筆記資料夾路徑
- [ ] 資源回收筒裡有那四個檔案,還原後四則都回來
- [ ] `readme.txt` 與空的子資料夾都還在(只刪 `.md`,不動資料夾)

空資料夾(這是原本會把 CmdPal 整個關掉的那條路):

- [ ] 資料夾一則筆記都沒有時執行 `Notelet:刪除筆記`
- [ ] **面板不會關掉**,頁面顯示「沒有筆記可以刪除」與資料夾路徑,沒有 toast

## 8. 設定

- [ ] 在 Notelet 上按 `Ctrl+K` → 設定,設定頁打開
- [ ] 有三個設定項,由上到下:筆記資料夾、快速記下的分隔符、記下後先看一眼
      (`QuickCaptureEnabled` / `QuickCapturePrefix` 隨 fallback 一起移除,
      `DetailsWidth` 隨面板寬度寫死一起移除。舊的 settings.json 裡可能還留著這幾個鍵,
      是孤兒,會被忽略;分隔符那個鍵舊檔案裡也不會有,讀不到就當預設值)
- [ ] 每個欄位上方是短標題(「筆記資料夾」),說明在欄位**下面**,按鈕寫的是「儲存」
- [ ] 卡片**最上面直接就是「筆記資料夾」那一格**,上頭沒有多一行說明
      (那句「換資料夾不會搬動已經寫好的筆記」已經併進資料夾欄位底下的註腳,
      每個欄位下面各一塊、沒有例外)
- [ ] 分隔符的輸入框**是窄的**(它只放得下兩三個字元),上方的標題**沒有折行**
- [ ] 分隔符的說明裡**看不到反引號**(Adaptive Cards 的 `TextBlock` 不認得它,
      會照字面印出來 —— 所以那段字用的是「」)
- [ ] 三個設定項之間都有分隔線
- [ ] 把資料夾改到另一個路徑,清單改讀新資料夾,新筆記寫進新資料夾
- [ ] 改完設定之後 `Notelet:快速記下` 照樣能用(整組 provider 會重建,頁面是新的那一個)
- [ ] 在資料夾輸入框裡按 Enter = 存檔(**不是**跳出瀏覽對話框)

**存完之後卡片不能停在舊值**(這條漏掉不會報錯,只會安靜地顯示舊值):

- [ ] 走 **設定 → Extensions → Notelet** 這個入口(另一個入口每次都重建,驗不到這條),
      把分隔符改成 `##` 存檔
- [ ] 離開這一頁(點左邊的 Installed)再回來 → 輸入框顯示的是 `##`,**不是**舊值
- [ ] `settings.json` 裡也是 `##`,兩邊一致
- [ ] **不會把值吃回去**:接著只改資料夾、按儲存 → 分隔符**仍然是** `##`
      (卡片要是停在舊值,那個舊值會被當成使用者的輸入寫回去,把 `##` 默默還原)
- [ ] 兩個入口顯示的值**永遠一致**(它們是同一個頁面實例)
- [ ] 改回自己慣用的分隔符收尾

**焦點**(表單後面那塊空白內容已經移除,這幾條是拿掉之後的預期行為):

- [ ] 打開設定頁,**游標自動落在「筆記資料夾」輸入框**,不必先點一下或按 Tab
      (這正是拿掉那塊空白換來的。之前它擋著自動聚焦)
- [ ] 設定頁上看不到多餘的空白區塊,說明文字全部貼在自己的欄位底下
- [ ] 對照組:`Notelet:新增筆記` 的表單游標一樣自動落在第一格

**觀察項,不是回歸**(那塊空白原本要擋的就是這個,但它依賴的 `OnlyControlOnPage`
在安裝版裡不存在 —— 見 README〈表單後面那塊空白已經拿掉了〉):

- [ ] 設定 → Extensions → Notelet 那一頁開著 → 切回 CmdPal 主視窗 → `Ctrl+K` → 設定 →
      改點東西按儲存 → 背景那個設定視窗**有沒有**跳到前面來?
      有的話記到下面的「記錄」裡:兩個入口共用同一個頁面實例,存檔後一律叫 CmdPal 重讀
      表單 = 控件重建 + 再一次 `Loaded`,而 `ContentFormControl` 載入後會自動聚焦。
      補救方式是讓 `GetContent()` 多回傳一塊內容 —— 但先 byte-scan 確認當時的安裝版
      到底有沒有那道判斷,不要照 CmdPal `main` 的原始碼寫

**全新安裝看到的預設值**(先把 `settings.json` 改名備份、Reload,驗完再改回來 ——
路徑見 README〈設定存在哪,更新擴展之後還在嗎〉):

- [ ] 「記下後先看一眼」一開就是**勾著的**
- [ ] 筆記資料夾是 `%OneDrive%\Notelet`,分隔符是 `;;`
- [ ] 詳細面板一開就是**寬**(1:1)

**舊設定不會被更新洗掉**(改預設值那次的回歸驗證):

- [ ] 設定頁把分隔符改成 `,,` 存檔 → 重新部署 + Reload → 還是 `,,`
      (預設值只對「檔案裡沒有那個鍵」的情況生效)
- [ ] `settings.json` 裡的孤兒鍵(`Notelet.QuickCapturePrefix`、`Notelet.DetailsWidth` 之類)
      存過檔之後**還在**,而且沒有影響任何設定項

### 8b. 「瀏覽…」按鈕

- [ ] 「瀏覽…」跟輸入框排在同一列,高度對齊
- [ ] 按下去跳出系統的選資料夾對話框,**開在最上面看得到**
      (**這一項最重要**:對話框掛在隱藏的 owner 底下,工作列上沒有它的按鈕 ——
      萬一它開在 CmdPal 後面,使用者等於完全找不到。拉不到前景是 Windows 的前景權限限制,
      見 README〈資料夾旁邊的「瀏覽…」〉)
- [ ] 工作列上**不會多出一個空白圖示的按鈕**
- [ ] 對話框一開始停在**目前設定的那個資料夾**
- [ ] 選一個資料夾按確定 → 跳出 toast「筆記資料夾:…」,**不必再按儲存**
      (CmdPal 主視窗這時多半已經自己藏起來了 —— 失焦就隱藏是它的行為,沒有開關)
- [ ] **壓在表單上的其他改動也一起存下來了**:先把分隔符改成 `,,`(不要按儲存)、
      直接按「瀏覽…」選一個資料夾 → 分隔符也變成 `,,` 了。表單會跟著主視窗一起消失,
      那些值只有這一次機會存下來(改回 `;;` 與原本的資料夾收尾)
- [ ] 重新叫出 CmdPal → 設定頁 → 輸入框顯示的是剛才選的路徑
- [ ] 清單頁列的是新資料夾裡的筆記
- [ ] 按取消 → 什麼都不變,設定檔沒有被改寫
- [ ] 兩個入口都要驗(`Ctrl+K` → 設定,以及設定 → Extensions → Notelet)

## 9. 外來檔案與同步

- [ ] 用文字編輯器在筆記資料夾放一個**沒有 front matter** 的 `.md`
      (第一行寫 `# 外面來的標題`)
- [ ] 它出現在 Notelet 清單裡,標題是「外面來的標題」
- [ ] 在子資料夾裡放一個 `.md`,它也出現在清單裡
- [ ] **即時更新**:Notelet 清單開著的時候,用編輯器改某則筆記的標題並存檔,
      清單在一兩秒內自己更新(這就是別台機器經 OneDrive 同步下來時會發生的事)

## 10. 錯誤處理

- [ ] 把設定裡的資料夾改成一個不存在的路徑,擴展不會壞掉,顯示空清單
- [ ] 在筆記資料夾放一個內容是亂碼、front matter 沒收尾的 `.md`,
      清單照樣列出其他筆記,不會整個掛掉

## 11. 介面語言

語言跟著 Windows 的顯示語言走,沒有設定項(見 README〈介面語言跟著 Windows 走〉)。
`.resx` 動過就要跑這一段 —— 少一條 key、佔位符對不上,單元測試擋得住;
但「翻譯是對的、位置卻塞不下」只有眼睛看得出來。

先確認擴展進程拿到的是哪一種語言(需要 `diagnostic.on`,見 README〈排錯〉):

```powershell
Get-Content "$env:LOCALAPPDATA\Packages\Notelet_bf0n0751x5hse\LocalState\diagnostic.log" -Encoding utf8 |
  Select-String "UI 語言"
```

- [ ] 印出 `UI 語言:zh-TW 抽樣='設定'`(抽樣是英文 `Settings` 代表附屬組件沒進套件)

**換一種語言實測**(改 Windows 設定 → 時間與語言 → 語言與地區 → Windows 顯示語言,
要重新登入;測完記得改回來):

- [ ] 改成英文之後,頂層四個命令變成 `Notelet` / `Notelet: Quick capture` /
      `Notelet: New note` / `Notelet: Delete notes`
- [ ] **alias 還在、還能用** —— 這是命令 Id 寫死的回歸測試,換語言不該讓使用者的
      alias 失效(標題變了,`Id` 沒變)
- [ ] 快速記下頁的提示句裡,分隔符還是使用者設的那個(不是寫死的 `;;`)
- [ ] 設定頁三個欄位的標籤與說明都是英文,而且**沒有一句被截斷或擠成兩行怪換行**
      (英文比中文長,卡片寬度是固定的)
- [ ] 刪除頁的「刪除全部」詳細窗格:數字、資料夾路徑都填對了,粗體與段落沒有跑掉
- [ ] 記下一則、刪一則,toast 與確認框的字都是英文,沒有中英夾雜
      (`ex.Message` 那段本來就是英文,那是預期的)

---

## 記錄

每個里程碑跑完把日期與結果記在這裡。

| 日期 | 組態 | 範圍 | 結果 |
|---|---|---|---|
| | | | |
