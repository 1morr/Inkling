bin/
obj/
.vs/
*.user
*.suo
TestResults/
AppPackages/
BundleArtifacts/

# 打包與簽章產出物。*.pfx / *.p12 裡是私鑰 —— 一個 git add . 就可能把它送進
# 公開 repo，事後刪檔也已外洩，憑證只能作廢重辦。要分享公開憑證指紋時
# 用 .cer 明確加入，不要靠萬用字元放行。
*.pfx
*.p12
*.msix
*.msixbundle
*.appx
*.appxbundle
bundle_mapping.txt

# release workflow 的暫存佈局與產出目錄(.github/workflows/release.yml)。
# CI 上是乾淨機器所以不影響，但在本機重現打包步驟時它會整包冒出來。
artifacts/

_ReSharper*/
*.DotSettings.user

# Claude Code 的本機設定。裡面會有這台機器專屬的東西(API endpoint、權限允許清單),
# 而且它是工具自己產生的 —— 不進版控。
.claude/settings.local.json

# 注意:標準 C# .gitignore 會排除下面兩者，但 Windows App SDK 需要它們才能部署套件。
# 一旦忽略，clone 這個 repo 的人(包含未來的你換機器)就無法部署擴展。
# 官方文檔明確要求把這兩行從 .gitignore 移除:
#   **/Properties/launchSettings.json
#   *.pubxml
